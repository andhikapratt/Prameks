<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="id.kuliah.prameksapp">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyTrips"></activity>
        <activity android:name=".DetailPembayaran" />
        <activity android:name=".Selesai" />
        <activity android:name=".Pembayaran" />
        <activity android:name=".TampilTiket" />
        <activity android:name=".ReviewTiket" />
        <activity android:name=".DataPenumpang" />
        <activity android:name=".Koneksi.activity_data_penumpang" />
        <activity android:name=".CariTiket" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>